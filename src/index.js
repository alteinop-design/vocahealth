import React from 'react';
import ReactDOM from 'react-dom/client';
import VocaHealth from './VocaHealth';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <VocaHealth />
  </React.StrictMode>
);
